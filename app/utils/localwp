#!/bin/bash

# Load configuration
root_path="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"; root_path=${root_path%app*}
source ${root_path}config

# Make new local WordPress site
cd ~/Sites
mkdir $1
cd $1
valet secure
wp core download
wp config create --dbname=$1 --dbuser=root --dbpass=$local_wp_db_pw
wp db create
wp core install --url=https://$1.anchor --admin_user=anchorhost --admin_email=support@anchor.host --title=$1
wp option update timezone_string America/New_York
open $(wp option get home)/wp-login.php
